<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/main.css" />
    <link rel="stylesheet" href="/stylesheets/login.css" />
    <script src="/javascripts/signup.js" defer></script>
    <title>Signup</title>
  </head>
  <body>
    <main>
      <form id="signup-form" novalidate action="/auth/signup" method="post">
        <p class="field">
          <label for="first-name">First Name</label>
          <input
            autocomplete="name"
            required
            type="text"
            id="first-name"
            name="first_name"
            value="<%=firstName%>"
            minlength="1"
            maxlength="60"
            pattern="[A-Za-z]+"
            aria-describedby="first-name-validation-message"
          />
          <span id="first-name-validation-message" aria-live="polite" class="validation-message"></span>
        </p>
        <p class="field">
          <label for="last-name">Last Name</label>
          <input
            autocomplete="family-name"
            required
            type="text"
            id="last-name"
            name="last_name"
            value="<%=lastName%>"
            minlength="1"
            maxlength="60"
            pattern="[A-Za-z]+"
            aria-describedby="last-name-validation-message"
          />
          <span id="last-name-validation-message" aria-live="polite" class="validation-message"></span>
        </p>
        <p class="field">
          <label for="username">Username</label>
          <input
            autocomplete="username"
            required
            type="text"
            id="username"
            name="username"
            value="<%=username%>"
            minlength="3"
            maxlength="35"
            pattern="[A-Za-z0-9]+"
            aria-describedby="username-validation-message"
          />
          <span id="username-validation-message" aria-live="polite" class="validation-message"></span>
        </p>
        <p class="field">
          <label for="password">Password</label>
          <input
            required
            autocomplete="new-password"
            type="password"
            id="password"
            name="password"
            value="<%=password%>"
            minlength="8"
            maxlength="128"
            pattern="^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$"
 
            
            aria-describedby="password-validation-message"
          />
          <span id="password-validation-message" aria-live="polite" class="validation-message"></span>
        </p>
        <p class="field">
          <label for="password-confirm">Confirm Password</label>
          <input
            required
            autocomplete="new-password"
            type="password"
            id="password-confirm"
            name="password_confirm"
            value="<%=passwordConfirm%>"
            aria-describedby="password-confirm-validation-message"
          />
          <span id="password-confirm-validation-message" aria-live="polite" class="validation-message"></span>
        </p>
        <div class="action-buttons">
          <button>Sign Up</button>
          <a href="/auth/login">I have an account</a>
        </div>
        <% if(errors.length!=0){%>

        <div class="errors">
          <ul>
            <% for(const error of errors){%>
              <li class="error">
                <img src="/images/cross.svg" alt="">
                <%=error.msg%>
              </li>
            <%}%>
          </ul>
        </div>
        
        <%}%>
      </form>
    </main>
  </body>
</html>
